export const metadata = {
  title: 'My Wishlist',
  description: 'Saved items in your BURHAN STORE wishlist.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function WishlistLayout({ children }) {
  return children;
}
